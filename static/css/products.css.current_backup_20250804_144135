/* Prima Arte - Estilos da Página de Produtos - Versão Profissional */

.products-section {
    padding: 2rem 0;
    background: #fafafa;
    min-height: 70vh;
}

.page-header {
    text-align: center;
    margin-bottom: 3rem;
    padding: 2rem 0;
    background: linear-gradient(135deg, rgba(218, 165, 32, 0.1), rgba(233, 30, 99, 0.1));
    border-radius: 20px;
}

.page-header h1 {
    font-family: var(--font-heading);
    font-size: 2.5rem;
    color: var(--dark-brown);
    margin-bottom: 1rem;
}

.page-header p {
    font-size: 1.2rem;
    color: #666;
    max-width: 600px;
    margin: 0 auto;
}

/* ================================
   FILTROS
   ================================ */
.filters {
    background: white;
    padding: 2rem;
    margin-bottom: 2rem;
    border-radius: 15px;
    box-shadow: 0 5px 20px rgba(0,0,0,0.05);
}

.filter-group {
    display: flex;
    align-items: center;
    gap: 1rem;
}

.filter-group label {
    font-weight: 600;
    color: var(--dark-brown);
    font-size: 1.1rem;
}

.filter-group select {
    padding: 0.8rem 1.5rem;
    border: 2px solid var(--primary-beige);
    border-radius: 10px;
    font-size: 1rem;
    color: var(--dark-brown);
    background: white;
    cursor: pointer;
    transition: border-color 0.3s ease;
}

.filter-group select:focus {
    outline: none;
    border-color: var(--primary-gold);
}

/* ================================
   GRID DE PRODUTOS
   ================================ */
.products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    align-items: stretch;
}

.product-card {
    background: white;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 8px 30px rgba(0,0,0,0.1);
    transition: all 0.3s ease;
    border: 1px solid rgba(0,0,0,0.05);
    display: grid;
    grid-template-rows: 300px 1fr;
    height: 100%;
}

.product-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 15px 40px rgba(0,0,0,0.15);
}

/* ================================
   IMAGEM DO PRODUTO
   ================================ */
.product-image {
    position: relative;
    overflow: hidden;
    grid-row: 1;
}

.product-image img {
    width: 100%;
    height: 300px;
    object-fit: cover;
    transition: transform 0.3s ease;
}

.product-card:hover .product-image img {
    transform: scale(1.1);
}

.no-image {
    width: 100%;
    height: 300px;
    background: linear-gradient(135deg, var(--primary-beige), #e0e0e0);
    display: flex;
    align-items: center;
    justify-content: center;
    color: #999;
    font-size: 3rem;
}

/* ================================
   BADGE DE PROMOÇÃO
   ================================ */
.promotion-badge {
    position: absolute;
    top: 15px;
    right: 15px;
    background: linear-gradient(135deg, #ff4444, #cc0000);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.9rem;
    font-weight: 700;
    box-shadow: 0 3px 15px rgba(255, 68, 68, 0.4);
    z-index: 2;
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
}

/* ================================
   INFORMAÇÕES DO PRODUTO
   ================================ */
.product-info {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    grid-row: 2;
    justify-content: space-between;
}

.product-title {
    font-family: var(--font-heading);
    font-size: 1.4rem;
    color: var(--dark-brown);
    line-height: 1.3;
    margin: 0;
    font-weight: 600;
}

.product-description {
    color: #666;
    line-height: 1.6;
    font-size: 0.95rem;
    margin: 0;
    flex-grow: 1;
}

/* ================================
   SISTEMA DE PREÇOS NA GRID
   ================================ */
.product-price-grid {
    margin: 1.5rem 0;
}

.price-with-promotion {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
}

.original-price {
    font-size: 1rem;
    color: #999;
    text-decoration: line-through;
    font-weight: 400;
}

.promotional-price {
    font-size: 1.8rem;
    color: var(--primary-pink);
    font-weight: 700;
}

.price-normal .current-price {
    font-size: 1.8rem;
    color: var(--primary-pink);
    font-weight: 700;
}

/* ================================
   AÇÕES DO PRODUTO
   ================================ */
.product-actions {
    margin-top: auto;
}

.btn-product-view {
    background: linear-gradient(135deg, var(--primary-gold), #ffa000);
    color: white;
    text-decoration: none;
    padding: 1rem 2rem;
    border-radius: 15px;
    font-weight: 600;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    width: 100%;
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    box-shadow: 0 5px 20px rgba(218, 165, 32, 0.3);
}

.btn-product-view:hover {
    background: linear-gradient(135deg, var(--primary-pink), #ad1457);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(233, 30, 99, 0.4);
}

/* ================================
   ESTADO VAZIO
   ================================ */
.empty-state {
    text-align: center;
    padding: 4rem 2rem;
    background: white;
    border-radius: 20px;
    box-shadow: 0 5px 20px rgba(0,0,0,0.05);
    grid-column: 1 / -1;
}

.empty-state i {
    font-size: 4rem;
    color: #ccc;
    margin-bottom: 1rem;
}

.empty-state h3 {
    font-size: 1.8rem;
    color: var(--dark-brown);
    margin-bottom: 1rem;
}

.empty-state p {
    font-size: 1.1rem;
    color: #666;
    margin-bottom: 2rem;
}

/* ================================
   RESPONSIVIDADE
   ================================ */
@media (max-width: 768px) {
    .products-grid {
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
    }
    
    .page-header {
        padding: 1.5rem;
        margin-bottom: 2rem;
    }
    
    .page-header h1 {
        font-size: 2rem;
    }
    
    .filters {
        padding: 1.5rem;
        margin-bottom: 1.5rem;
    }
    
    .filter-group {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.5rem;
    }
    
    .filter-group select {
        width: 100%;
    }
    
    .product-info {
        padding: 1.5rem;
    }
    
    .product-title {
        font-size: 1.2rem;
    }
    
    .promotional-price,
    .current-price {
        font-size: 1.5rem !important;
    }
}

@media (max-width: 480px) {
    .products-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    
    .product-card {
        grid-template-rows: 250px 1fr;
    }
    
    .product-image img,
    .no-image {
        height: 250px;
    }
    
    .page-header h1 {
        font-size: 1.8rem;
    }
    
    .page-header p {
        font-size: 1rem;
    }
    
    .empty-state {
        padding: 3rem 1rem;
    }
    
    .empty-state i {
        font-size: 3rem;
    }
    
    .empty-state h3 {
        font-size: 1.5rem;
    }
}