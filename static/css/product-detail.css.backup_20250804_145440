/* Prima Arte - Estilos da Página Individual do Produto (Melhorado) */

/* ================================
   LAYOUT PRINCIPAL
   ================================ */
.product-detail {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
    max-width: 1400px;
    margin: 0 auto;
}

/* ================================
   GALERIA DE IMAGENS
   ================================ */
.product-gallery {
    position: sticky;
    top: 2rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.main-image-container {
    position: relative;
    margin-bottom: 1rem;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 15px 50px rgba(0,0,0,0.15);
    background: white;
}

.main-image {
    width: 100%;
    height: 500px;
    object-fit: cover;
    cursor: zoom-in;
    transition: transform 0.3s ease;
}

.main-image-placeholder {
    width: 100%;
    height: 500px;
    background: var(--primary-beige);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: var(--gray);
    font-size: 3rem;
}

.main-image-placeholder p {
    margin-top: 1rem;
    font-size: 1.2rem;
}

.main-image:hover {
    transform: scale(1.02);
}

.zoom-indicator {
    position: absolute;
    bottom: 15px;
    right: 15px;
    background: rgba(0,0,0,0.8);
    color: white;
    padding: 10px 15px;
    border-radius: 25px;
    font-size: 0.9rem;
    display: flex;
    align-items: center;
    gap: 8px;
    backdrop-filter: blur(10px);
    opacity: 0;
    transition: opacity 0.3s ease;
    font-weight: 500;
}

.main-image-container:hover .zoom-indicator {
    opacity: 1;
}

.promotion-badge-detail {
    position: absolute;
    top: 20px;
    left: 20px;
    background: linear-gradient(135deg, #FF6B6B, #FF4757);
    color: white;
    padding: 12px 20px;
    border-radius: 25px;
    font-size: 1rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 8px;
    box-shadow: 0 8px 25px rgba(255, 107, 107, 0.4);
    z-index: 2;
    animation: promotionPulse 2s infinite;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

@keyframes promotionPulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
}

.image-thumbnails {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(80px, 1fr));
    gap: 0.8rem;
}

.thumbnail {
    width: 100%;
    height: 80px;
    object-fit: cover;
    border-radius: 12px;
    cursor: pointer;
    border: 3px solid transparent;
    transition: all 0.3s ease;
}

.thumbnail:hover,
.thumbnail.active {
    border-color: var(--primary-gold);
    transform: scale(1.05);
    box-shadow: 0 0 20px rgba(218, 165, 32, 0.4);
}

/* ================================
   INFORMAÇÕES DO PRODUTO
   ================================ */
.product-info {
    padding: 1rem 0;
    display: flex;
    flex-direction: column;
    gap: 2rem;
}

.product-header {
    border-bottom: 2px solid var(--primary-beige);
    padding-bottom: 1.5rem;
}

.product-title {
    font-family: var(--font-heading);
    font-size: 2.8rem;
    color: var(--dark-brown);
    margin: 0 0 1rem 0;
    line-height: 1.2;
}

.product-category {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--primary-beige);
    color: var(--dark-brown);
    padding: 8px 16px;
    border-radius: 20px;
    font-weight: 600;
    font-size: 0.9rem;
}

/* ================================
   SEÇÃO DE PREÇOS
   ================================ */
.product-pricing {
    background: linear-gradient(135deg, #f8f9fa, #e9ecef);
    padding: 2rem;
    border-radius: 20px;
    border: 2px solid var(--primary-beige);
}

.pricing-promotion .regular-price-detail {
    margin-bottom: 0.5rem;
}

.pricing-promotion .regular-price-detail .price-label {
    font-size: 1rem;
    color: var(--gray);
    display: block;
    margin-bottom: 0.3rem;
}

.pricing-promotion .regular-price-detail .price-value {
    font-size: 1.4rem;
    color: #999;
    text-decoration: line-through;
    font-weight: 500;
}

.pricing-promotion .promotional-price-detail {
    margin-bottom: 1rem;
}

.pricing-promotion .promotional-price-detail .price-label {
    font-size: 1.1rem;
    color: var(--dark-brown);
    font-weight: 600;
    display: block;
    margin-bottom: 0.5rem;
}

.pricing-promotion .promotional-price-detail .price-value {
    font-size: 3rem;
    color: #4CAF50;
    font-weight: 800;
    display: block;
    line-height: 1;
}

.pricing-regular .regular-price-only .price-label {
    font-size: 1.2rem;
    color: var(--dark-brown);
    font-weight: 600;
    display: block;
    margin-bottom: 0.5rem;
}

.pricing-regular .regular-price-only .price-value {
    font-size: 2.5rem;
    color: var(--primary-pink);
    font-weight: 800;
    display: block;
    line-height: 1;
}

.savings-info {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-top: 1rem;
}

.savings-amount {
    background: #4CAF50;
    color: white;
    padding: 12px 16px;
    border-radius: 15px;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.9rem;
}

.promotion-timer {
    background: #FF6B6B;
    color: white;
    padding: 12px 16px;
    border-radius: 15px;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.9rem;
    animation: urgency 2s infinite;
}

@keyframes urgency {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.8; }
}

/* ================================
   DESCRIÇÃO E CARACTERÍSTICAS
   ================================ */
.product-description,
.product-features {
    background: white;
    padding: 1.5rem;
    border-radius: 15px;
    border: 1px solid var(--primary-beige);
}

.product-description h3,
.product-features h3 {
    font-family: var(--font-heading);
    color: var(--dark-brown);
    margin: 0 0 1rem 0;
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 1.4rem;
}

.product-description p {
    color: var(--gray);
    line-height: 1.7;
    margin: 0;
    font-size: 1.1rem;
}

.features-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
}

.feature-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 12px;
    background: var(--primary-beige);
    border-radius: 10px;
    font-weight: 600;
    color: var(--dark-brown);
}

.feature-item i {
    color: var(--primary-gold);
    font-size: 1.2rem;
}

/* ================================
   FORMULÁRIO DE CONTATO
   ================================ */
.product-contact {
    background: white;
    padding: 2rem;
    border-radius: 20px;
    border: 2px solid var(--primary-gold);
    box-shadow: 0 10px 30px rgba(218, 165, 32, 0.1);
}

.product-contact h3 {
    font-family: var(--font-heading);
    color: var(--dark-brown);
    margin: 0 0 1.5rem 0;
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 1.5rem;
}

.contact-options {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-bottom: 2rem;
}

.btn-whatsapp-large {
    background: linear-gradient(135deg, #25D366, #22C55E);
    color: white;
    padding: 1rem 1.5rem;
    border-radius: 15px;
    text-decoration: none;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    transition: all 0.3s ease;
    font-size: 1rem;
}

.btn-whatsapp-large:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 25px rgba(37, 211, 102, 0.3);
}

.btn-info-large {
    background: linear-gradient(135deg, var(--primary-teal), #38BDF8);
    color: white;
    padding: 1rem 1.5rem;
    border-radius: 15px;
    border: none;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    transition: all 0.3s ease;
    font-size: 1rem;
    cursor: pointer;
    font-family: var(--font-body);
}

.btn-info-large:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 25px rgba(38, 198, 218, 0.3);
}

/* ================================
   FORMULÁRIO DE ORÇAMENTO
   ================================ */
.order-form {
    background: #f8f9fa;
    padding: 1.5rem;
    border-radius: 15px;
    margin-top: 1rem;
    border: 1px solid #e9ecef;
}

.order-form h4 {
    margin: 0 0 1rem 0;
    color: var(--dark-brown);
    display: flex;
    align-items: center;
    gap: 8px;
}

.form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-bottom: 1rem;
}

.order-form input,
.order-form select,
.order-form textarea {
    padding: 12px;
    border: 2px solid var(--primary-beige);
    border-radius: 10px;
    font-family: var(--font-body);
    font-size: 1rem;
}

.order-form textarea {
    grid-column: 1 / -1;
    resize: vertical;
}

.btn-submit {
    background: linear-gradient(135deg, var(--primary-pink), #EC4899);
    color: white;
    padding: 12px 24px;
    border: none;
    border-radius: 10px;
    font-weight: 600;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: var(--font-body);
    font-size: 1rem;
    transition: all 0.3s ease;
}

.btn-submit:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(236, 72, 153, 0.3);
}

/* ================================
   INFORMAÇÕES DE ENTREGA
   ================================ */
.delivery-info {
    background: white;
    padding: 1.5rem;
    border-radius: 15px;
    border: 1px solid var(--primary-beige);
}

.delivery-info h3 {
    font-family: var(--font-heading);
    color: var(--dark-brown);
    margin: 0 0 1rem 0;
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 1.4rem;
}

.delivery-options {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.delivery-item {
    display: flex;
    align-items: center;
    gap: 15px;
    padding: 15px;
    background: var(--primary-beige);
    border-radius: 10px;
}

.delivery-item i {
    color: var(--primary-gold);
    font-size: 1.5rem;
    width: 30px;
    text-align: center;
}

.delivery-item div {
    display: flex;
    flex-direction: column;
}

.delivery-item strong {
    color: var(--dark-brown);
    font-size: 1rem;
    margin-bottom: 3px;
}

.delivery-item span {
    color: var(--gray);
    font-size: 0.9rem;
}

/* ================================
   ZOOM DA IMAGEM
   ================================ */
.image-zoom-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.9);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    cursor: pointer;
}

.zoom-container {
    position: relative;
    max-width: 90vw;
    max-height: 90vh;
}

.zoom-container img {
    max-width: 100%;
    max-height: 100%;
    border-radius: 10px;
}

.zoom-close {
    position: absolute;
    top: -15px;
    right: -15px;
    background: white;
    border: none;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    font-size: 1.5rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 5px 15px rgba(0,0,0,0.3);
}

/* ================================
   RESPONSIVIDADE
   ================================ */
@media (max-width: 968px) {
    .product-detail {
        grid-template-columns: 1fr;
        gap: 2rem;
    }
    
    .product-gallery {
        position: relative;
        top: 0;
    }
    
    .main-image {
        height: 400px;
    }
    
    .product-title {
        font-size: 2.2rem;
    }
    
    .pricing-promotion .promotional-price-detail .price-value {
        font-size: 2.5rem;
    }
    
    .contact-options {
        grid-template-columns: 1fr;
    }
    
    .form-row {
        grid-template-columns: 1fr;
    }
    
    .features-grid {
        grid-template-columns: 1fr;
    }
    
    .savings-info {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 576px) {
    .product-title {
        font-size: 1.8rem;
    }
    
    .main-image {
        height: 300px;
    }
    
    .pricing-promotion .promotional-price-detail .price-value {
        font-size: 2rem;
    }
    
    .product-contact,
    .product-pricing {
        padding: 1.5rem;
    }
}